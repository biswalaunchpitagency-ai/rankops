VITE_SENTRY_DSN="" # Optional: Sentry error tracking DSN
VITE_SENTRY_ENVIRONMENT="" # e.g. development, staging, production

# Build-time only (CI):
# SENTRY_AUTH_TOKEN=""
# SENTRY_ORG=""
# SENTRY_PROJECT=""

# API endpoint (only required if deployed separately from server, e.g. on Vercel):
VITE_API_URL=""

